<nav class="navbar navbar-expand-md border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">
      <i class="bi bi-mortarboard"></i>EduRadar
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
      data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" 
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <div class="navbar-nav">
        <a class="nav-link <% if(activePage === 'home'){ %>active<% } %>" href="/">
          <i class="bi bi-house-door"></i>Home
        </a>
        <a class="nav-link <% if(activePage === 'listings'){ %>active<% } %>" href="/listings">
          <i class="bi bi-card-list"></i>All Listings
        </a>
        <a class="nav-link <% if(activePage === 'create'){ %>active<% } %>" href="/listings/new">
          <i class="bi bi-plus-circle"></i>New Listing
        </a>
      </div>
    </div>
  </div>
</nav>
